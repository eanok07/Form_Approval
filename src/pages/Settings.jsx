import React from 'react';

const Settings = () => {
    return (
        <div>
            <h1>Settings</h1>
            <p>This is the settings page where users can configure application settings.</p>
        </div>
    );
};

export default Settings;