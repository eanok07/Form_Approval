import React from 'react';

const Approvals = () => {
    return (
        <div>
            <h1>Pending Approvals</h1>
            {/* Add logic to display pending approval requests here */}
        </div>
    );
};

export default Approvals;